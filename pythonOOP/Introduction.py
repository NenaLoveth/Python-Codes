class corper:
    def __init__(self, name, call_up_number, state_of_deploy, state_code, ppa):
        self.name = name
        self.call_up_number = call_up_number
        self.state_of_deploy = state_of_deploy
        self.state_code = state_code
        self.ppa = ppa

    def status():
        if pop == True:
            print("Congrats")
        else:
            print("still in service")


corper1 = corper("Loveth", 284447, "Nasarawa", "NS/19c/0644", "COHTSK")
print(corper1.state_code)
corper(False)
